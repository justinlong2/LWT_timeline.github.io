# Theme Settings
remote_theme: just-the-docs/just-the-docs

# Site Settings
title: Project Dashboard
description: Expert Review & LWT Schedule

# Enable Mermaid globally
mermaid:
  version: "10" # This ensures the JS is loaded automatically

# Layout Settings
# This makes the content area wider by default
aux_links:
  "View on GitHub":
    - https://github.com/your-username/your-repo
